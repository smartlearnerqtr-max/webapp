# Ghi chú nhanh sau khi xem thư mục `images`

## Tổng quan

- Thư mục `images` hiện có 11 ảnh chụp màn hình/demo của giao diện học tập.
- Ảnh chủ yếu mô tả luồng sau đăng nhập của học sinh: chọn mức độ hỗ trợ, chọn môn học, vào bài học, phần giao tiếp, trò chơi và mục nghề nghiệp.
- Các ảnh là ảnh chụp từ màn hình laptop và có vẻ lấy từ một video demo, vì nhiều ảnh còn hiện thanh thời lượng video, con trỏ chuột và viền màn hình.

## Những màn hình nhìn thấy được

### 1. `giao diện sau khi đăng nhập.jpg`

- Màn hình chào người dùng mới với tiêu đề `Smart Learner`.
- Có 3 lựa chọn mức độ:
- `MỨC ĐỘ NẶNG` - gợi ý `Hình ảnh & Âm thanh`
- `MỨC ĐỘ VỪA` - gợi ý `Sơ đồ tư duy`
- `MỨC ĐỘ NHẸ` - gợi ý `Lộ trình chuẩn`
- Bên dưới có mục `Dành cho Người lớn`.
- Thiết kế dùng màu sắc rất mạnh, bo tròn lớn, hướng tới trẻ em hoặc người cần hỗ trợ trực quan.

### 2. `giao diện sau khi chọn mức độ.jpg`

- Sau khi chọn mức độ, hệ thống vào tab `HỌC TẬP`.
- Có thanh tab trên cùng gồm:
- `HỌC TẬP`
- `BẠN HỌC AI`
- `GIAO TIẾP`
- Có banner chào mừng học sinh: `Chào mừng Lê Văn C!`
- Banner hiển thị thêm tiến độ/điểm:
- `5 Bài học hoàn thành`
- `120 Điểm thưởng`
- Bên dưới bắt đầu tới khu vực `CHỌN MÔN HỌC`.

### 3. `giao diện khi chọn mức độ.jpg`

- Thấy rõ dashboard học tập sau khi vào hệ thống.
- Có nút `Điều hướng giọng nói`.
- Danh sách môn học hàng đầu gồm:
- `Toán học`
- `Ngữ văn`
- `Tiếng Anh`
- `Khoa học tự nhiên`
- `Công nghệ`
- Mỗi môn hiển thị bằng thẻ lớn có icon minh họa riêng.

### 4. `giao diện khi chọn mức độ phần dưới.jpg`

- Phần dưới của danh sách môn học cho thấy còn nhiều môn khác:
- `Tin học` - có nhãn `Sắp ra mắt`
- `Lịch sử - Địa lý`
- `Giáo dục công dân`
- `Giáo dục địa phương` - có nhãn `Sắp ra mắt`
- `Hoạt động trải nghiệm` - có nhãn `Sắp ra mắt`
- Có thể thấy hệ thống đang định hướng mở rộng nhiều môn, nhưng một số mục vẫn chưa hoàn thiện.

### 5. `tiếp theo.jpg`

- Khu vực thấp hơn của trang chủ học tập cho thấy thêm các danh mục:
- `Mỹ thuật`
- `Âm nhạc` - `Sắp ra mắt`
- `Thể dục` - `Sắp ra mắt`
- `Kỹ năng sống` - `Sắp ra mắt`
- Xuất hiện thêm các section:
- `TRÒ CHƠI`
- `NGHỀ NGHIỆP`
- Ảnh này cho thấy trang chủ khá dài, chia thành nhiều nhóm nội dung chứ không chỉ có môn học cơ bản.

### 6. `môn học.jpg`

- Người dùng đã vào một bài học thuộc `Lịch sử - Địa lý`.
- Tiêu đề bài: `Lịch sử - Địa lý: Lá cờ Việt Nam`.
- Có nút `Quay lại`.
- Giao diện bài học chia làm 2 vùng:
- Bên trái là lời hướng dẫn bằng chữ và có nút loa để phát âm thanh.
- Bên phải là khung media/hình ảnh.
- Có các bước hoặc chế độ như:
- `1 QUAN SÁT`
- `2 LẮNG NGHE`
- Đây là kiểu bài học có hướng dẫn từng bước, khá phù hợp với học sinh cần nhịp học chậm.

### 7. `môn học ví dụ.jpg`

- Một ví dụ bài học trong môn `KHTN: Màu sắc cơ bản`.
- Yêu cầu hiển thị: `Chạm vào hình màu ĐỎ`.
- Có hai hình lựa chọn.
- Hệ thống phản hồi đúng bằng vòng tròn/ngôi sao lớn với chữ `GIỎI QUÁ!`
- Một thẻ ảnh có viền xanh và dấu check, cho thấy đã chọn đúng.
- Đây là dạng hoạt động tương tác đơn giản, phản hồi trực tiếp, dễ hiểu.

### 8. `phần giao tiếp với AI.jpg`

- Tab đang mở là `GIAO TIẾP`.
- Màn hình hiển thị các thẻ từ/cảm xúc/giao tiếp cơ bản có ảnh và nút âm thanh.
- Các thẻ nhìn thấy được gồm:
- `ĐÓI`
- `KHÁT`
- `ĐI VỆ SINH`
- `VUI`
- `BUỒN`
- `MỆT`
- `CHÀO`
- `CẢM ƠN`
- Chức năng này có vẻ là bảng giao tiếp AAC hoặc bảng hỗ trợ diễn đạt nhu cầu cơ bản.

### 9. `ví dụ khi truy cập chức năng trò chơi.jpg`

- Đây là màn `TRÒ CHƠI`.
- Có nút `Thoát`, bộ đếm `Lượt đi: 0` và nút `Chơi lại`.
- Nội dung đang hiển thị giống trò lật thẻ/ghi nhớ với 4 ô úp.
- Giao diện rất tối giản, tập trung vào trò chơi chính.

### 10. `nghề nghiệp.jpg`

- Đang ở mục nghề nghiệp, phần đầu có tiêu đề `Làm vườn`.
- Có khối `Ý nghĩa công việc`.
- Có khu vực `Video giải thích công việc`.
- Video hiện lỗi `Video unavailable`.
- Đây là một điểm cần chú ý vì nội dung media đang không hoạt động trong ảnh demo.

### 11. `mục nghề nghiệp.jpg`

- Phần tiếp theo của trang nghề nghiệp hiển thị nội dung hướng dẫn thao tác.
- Có một đoạn mô tả ngắn về tác dụng của video.
- Có mục `Các bước thực hiện` với trình tự:
- `1 Chuẩn bị bình tưới` - `Lấy nước đầy bình.`
- `2 Tưới gốc cây` - `Tưới nhẹ nhàng vào gốc.`
- `3 Cất bình` - `Để bình lại chỗ cũ.`
- Đây là hướng tiếp cận tốt cho phần kỹ năng nghề: chia nhỏ việc thành bước rõ ràng.

## Nhận xét thêm

- Luồng sản phẩm nhìn chung khá rõ: chọn mức độ -> vào học tập -> chọn môn/bài -> thực hiện tương tác -> có thêm giao tiếp, trò chơi, nghề nghiệp.
- Ngôn ngữ thiết kế nghiêng mạnh về trực quan, icon lớn, màu tươi, phù hợp đối tượng học sinh cần hỗ trợ.
- Hệ thống có vẻ đang kết hợp nhiều kiểu nội dung:
- bài học học thuật
- giao tiếp hằng ngày
- trò chơi luyện tập
- định hướng kỹ năng nghề
- Có một số dấu hiệu nội dung demo/chưa hoàn chỉnh:
- nhiều mục ghi `Sắp ra mắt`
- video nghề nghiệp bị lỗi
- ảnh trong bài `Lá cờ Việt Nam` đang hiển thị hình không khớp nội dung bài
- Vì ảnh là chụp lại từ video chạy trên laptop, nên hiện tại chưa phản ánh chính xác pixel-perfect UI; nếu dùng làm tài liệu đặc tả giao diện thì nên chụp screenshot trực tiếp từ app.

## Đối chiếu với dự án hiện tại

### Những gì dự án đã có sẵn và có thể tận dụng

- Dự án đã có nền `visual_support` cho lớp học sinh, tức là backend đã hiểu khái niệm lớp trực quan và theme giao diện.
- Backend đã có `ui_variant`, `visual_theme`, `background_image_url` trong lớp học.
- Frontend học sinh hiện đã có giao diện riêng cho kiểu `visual_support`, không còn là giao diện text-only cơ bản.
- `StudentHomePage` đã có nhiều thành phần phù hợp với hướng demo:
- bài học trực quan
- hướng dẫn bằng audio
- tổng hợp tiến độ
- AI chat
- voice reply
- hướng nghiệp bằng giọng nói
- `ActivityRenderer` đã hỗ trợ khá nhiều loại hoạt động gần với demo:
- chọn đáp án
- chọn ảnh
- ghép ảnh
- AAC
- lật thẻ ghi nhớ
- chạm nhanh
- sắp xếp
- ghép nơi sống
- mô phỏng tình huống nghề nghiệp
- Backend đã có sẵn TTS tiếng Việt và frontend đã gọi được audio AI.
- Hệ thống đã có seed demo `visual support`, tức là có thể tiếp tục mở rộng theo hướng dữ liệu mẫu trực quan thay vì làm lại từ đầu.

### Những khoảng cách lớn giữa bộ ảnh demo và code hiện tại

#### 1. Luồng vào của học sinh chưa giống demo

- Ảnh demo có bước `chọn mức độ nặng / vừa / nhẹ` ngay sau đăng nhập.
- Dự án hiện tại chưa có một màn onboarding riêng cho học sinh để chọn kiểu hỗ trợ này.
- Hệ thống hiện có `disability_level` trong hồ sơ học sinh, nhưng chưa dùng nó như một bước chọn giao diện đầu vào sau đăng nhập.

#### 2. Điều hướng học sinh hiện tại khác hẳn điều hướng trong ảnh

- Ảnh demo dùng 3 tab chính:
- `HỌC TẬP`
- `BẠN HỌC AI`
- `GIAO TIẾP`
- `StudentHomePage` hiện tại lại đang đi theo menu:
- `Trang chủ`
- `Tiến độ`
- `Thông tin`
- `Nghề`
- `Nhắc`
- `Cài đặt`
- Tức là nền chức năng có một phần đã có, nhưng kiến trúc trải nghiệm học sinh chưa khớp hướng sản phẩm trong ảnh.

#### 3. Trang chủ học sinh trong code đang thiên về assignment, chưa phải dashboard môn học

- Demo cho thấy học sinh vào là thấy:
- banner chào mừng
- thống kê sao/điểm
- lưới môn học
- khu trò chơi
- khu nghề nghiệp
- Hiện tại trang học sinh của dự án đang lấy trọng tâm là danh sách assignment và tiến độ bài được giao.
- Nghĩa là nếu đi theo demo, phải đổi từ `assignment-first` sang `student hub / subject-first`.

#### 4. Dữ liệu môn học hiện tại còn quá ít

- Seed backend mới có các môn nền:
- `Toán`
- `Văn`
- `Khoa học tự nhiên`
- `Kỹ năng sống`
- Trong ảnh demo có nhiều môn hơn:
- `Ngữ văn`
- `Tiếng Anh`
- `Khoa học tự nhiên`
- `Công nghệ`
- `Tin học`
- `Lịch sử - Địa lý`
- `Giáo dục công dân`
- `Giáo dục địa phương`
- `Hoạt động trải nghiệm`
- `Mỹ thuật`
- `Âm nhạc`
- `Thể dục`
- Vì vậy cần mở rộng catalog môn học và metadata hiển thị của từng môn.

#### 5. Phần giao tiếp trong code mới tồn tại ở mức activity, chưa là khu chức năng độc lập

- Demo có một tab `GIAO TIẾP` riêng với các thẻ giao tiếp hằng ngày.
- Trong code hiện tại, AAC chủ yếu tồn tại như một loại activity trong bài học.
- Điều còn thiếu là một trang giao tiếp độc lập luôn sẵn sàng cho học sinh, không phụ thuộc vào việc có assignment hay không.

#### 6. Trò chơi trong code hiện tại là activity, chưa là khu game launcher riêng

- Demo có khu `TRÒ CHƠI` riêng với các card game như `Tìm cặp hình giống nhau`, `Thử thách trí nhớ`.
- Trong code hiện tại, các game như `memory_match`, `quick_tap`, `size_order` đang gắn trong bài học.
- Nếu muốn giống demo, cần thêm lớp điều hướng riêng để học sinh mở trò chơi như một nội dung độc lập.
- Chốt cho hướng triển khai mới:
- `memory_match` được đưa vào mục `Trò chơi`.
- `quick_tap` được đưa vào mục `Trò chơi`.
- Hai activity này không xem là phần bài học chính trong hướng giao diện học sinh mới.

#### 7. Nghề nghiệp trong code hiện tại lệch hướng so với ảnh

- Hiện tại phần `career` trong `StudentHomePage` đang thiên về chat voice với AI hướng nghiệp.
- Demo lại cho thấy phần nghề nghiệp có:
- danh sách nghề
- trang chi tiết nghề
- ý nghĩa công việc
- video
- các bước thực hiện
- kỹ năng học được
- Tức là phần hướng nghiệp hiện tại chưa phải dạng thư viện nghề nghiệp trực quan như trong ảnh.

#### 8. Layout bài học hiện tại chưa có “lesson shell” đúng kiểu demo

- Demo của bài `Lá cờ Việt Nam` và `Màu sắc cơ bản` có bố cục rất rõ:
- tiêu đề bài lớn
- chỉ dẫn từng bước
- media nổi bật
- nút âm thanh
- phản hồi đúng kiểu “Giỏi quá”
- Hệ thống hiện đã có activity renderer mạnh, nhưng chưa có một vỏ giao diện bài học cố định, nhất quán theo mẫu demo cho các bài trực quan.

#### 9. CMS/lesson editor hiện tại chưa quản lý được toàn bộ nội dung kiểu demo

- `LessonsPage` hiện mạnh ở việc tạo activity.
- Nhưng để làm giống demo, giáo viên/admin sẽ còn cần quản lý:
- môn học hiển thị ngoài dashboard
- icon môn học
- trạng thái `Sắp ra mắt`
- bộ thẻ giao tiếp
- game card
- nhóm nghề nghiệp
- nội dung chi tiết nghề
- video nghề
- thứ tự hiển thị section trên home học sinh
- Tức là editor hiện tại chưa đủ scope cho mô hình sản phẩm mới.

### Những việc cần làm nếu muốn đổi dự án theo hướng bộ ảnh demo

## 1. Chốt lại kiến trúc trải nghiệm học sinh

- Quyết định rõ sản phẩm mới có dùng 3 tab chính như demo hay không.
- Khuyến nghị: chuyển `StudentHomePage` sang 3 khu chính:
- `Học tập`
- `Bạn học AI`
- `Giao tiếp`
- Các phần `trò chơi`, `nghề nghiệp`, `nhắc việc`, `vào lớp` có thể nằm bên trong `Học tập` hoặc trong các khối phụ.
- Nếu không chốt IA trước, rất dễ sửa UI nửa chừng rồi phải làm lại.

## 2. Thêm bước chọn mức độ hỗ trợ sau đăng nhập

- Tạo một màn onboarding riêng cho học sinh sau login.
- Cho chọn:
- `nặng`
- `vừa`
- `nhẹ`
- Lưu lựa chọn này vào hồ sơ học sinh hoặc bảng settings riêng.
- Dùng lựa chọn này để điều khiển:
- theme
- mức độ rút gọn UI
- kiểu hiển thị activity
- mức dùng audio / hình ảnh / chữ

### Gợi ý kỹ thuật

- Có thể bắt đầu bằng cách mở rộng `StudentProfile` hoặc thêm bảng `student_ui_preferences`.
- Frontend cần chèn bước này vào flow sau login trước khi vào `StudentHomePage`.

## 3. Refactor lớn `StudentHomePage`

- Đây sẽ là file đụng mạnh nhất.
- Cần đổi từ màn hình thiên về assignment sang màn hình hub trực quan.
- Nên tách nhỏ thành nhiều component thay vì tiếp tục dồn vào một file.

### Nên tách thành

- `StudentLevelChooser`
- `StudentLearningHub`
- `StudentSubjectGrid`
- `StudentCommunicationBoard`
- `StudentGameHub`
- `StudentCareerHub`
- `StudentCareerDetail`
- `StudentAIAssistantPanel`

### Việc cụ thể

- thay thanh menu hiện tại bằng top tabs theo demo
- thêm banner chào mừng, điểm thưởng, số bài hoàn thành
- thêm lưới môn học lớn có icon/card
- thêm section `Trò chơi`
- thêm section `Nghề nghiệp`
- thêm nút `Điều hướng giọng nói`

## 4. Bổ sung model dữ liệu cho dashboard môn học

- Subject hiện tại mới có:
- code
- name
- description
- sort_order
- is_active
- Cần thêm metadata để hiển thị như demo:
- icon hoặc illustration
- màu chủ đạo
- ảnh nền
- nhãn `Sắp ra mắt`
- loại nội dung
- thứ tự hiển thị trên student home

### Gợi ý

- Mở rộng bảng `subjects`, hoặc
- thêm bảng `subject_display_config`

## 5. Mở rộng seed dữ liệu và nội dung mẫu

- Cần seed thêm nhiều môn đúng theo hướng demo.
- Cần seed bài học minh họa thật sự khớp nội dung.
- Ví dụ:
- `Lá cờ Việt Nam` phải có ảnh/video đúng lá cờ Việt Nam
- `Màu sắc cơ bản` phải có bộ ảnh màu đúng
- các môn `Sắp ra mắt` có thể vẫn seed ở trạng thái disabled để hiện trên dashboard

### Cần làm

- cập nhật `seed_subjects`
- thêm seed icon/ảnh nền cho môn
- thêm seed bài học demo trực quan
- thêm seed game demo
- thêm seed nghề nghiệp demo

## 6. Tạo khu `Giao tiếp` độc lập

- Tách giao tiếp ra khỏi lesson activity.
- Tạo một trang cố định để học sinh bấm vào câu/thẻ giao tiếp cơ bản.
- Mỗi thẻ nên có:
- ảnh
- nhãn
- audio phát sẵn
- category

### Dữ liệu cần có

- danh mục thẻ giao tiếp
- nhóm thẻ
- thứ tự
- media_url
- audio_url tùy chọn

### Có thể tận dụng

- logic AAC hiện có trong `ActivityRenderer`
- TTS tiếng Việt đang có sẵn nếu chưa seed audio thu sẵn

## 7. Tạo khu `Trò chơi` độc lập

- Không nên chỉ dựa vào activity trong lesson nếu muốn giống demo.
- Nên có danh sách game riêng để học sinh vào chơi trực tiếp.

### Có thể tái sử dụng

- `memory_match`
- `quick_tap`
- `size_order`
- `habitat_match`
- Ưu tiên phân loại:
- `memory_match` -> mục `Trò chơi`
- `quick_tap` -> mục `Trò chơi`

### Cần thêm

- màn launcher game
- card game
- mô tả ngắn
- reset / replay flow rõ ràng
- lưu điểm nếu cần

## 8. Làm lại phần `Nghề nghiệp` theo dạng thư viện nội dung

- Phần này hiện cần đổi hướng đáng kể.
- Nên tách `AI hướng nghiệp bằng giọng nói` thành tính năng phụ.
- Phần chính phải là thư viện nghề trực quan như trong ảnh.

### Cần có

- danh sách nghề
- card nghề
- ảnh đại diện
- mô tả ngắn
- trang chi tiết nghề
- ý nghĩa công việc
- video giải thích
- các bước thực hiện
- kỹ năng học được

### Gợi ý model mới

- `CareerTopic`
- `CareerStep`
- `CareerSkill`
- `CareerMedia`

### Lưu ý

- Video phải kiểm tra lại nguồn nhúng để tránh lỗi `Video unavailable`.
- Nếu dùng YouTube/Drive, cần normalize URL và test thực tế.

## 9. Bổ sung “lesson shell” cho bài học trực quan

- Không chỉ render activity rời rạc.
- Cần một khung bài học thống nhất để bài nào cũng có cảm giác giống sản phẩm demo.

### Thành phần nên có

- tiêu đề bài
- avatar hoặc mascot
- ô hướng dẫn lớn
- khung media nổi bật
- step chips như `Quan sát`, `Lắng nghe`, `Chạm vào`
- phản hồi đúng/sai thật rõ
- hiệu ứng thưởng kiểu `Giỏi quá`

### Có thể tận dụng

- `ActivityRenderer` cho phần tương tác lõi
- `synthesizeAISpeech` cho phát chỉ dẫn

## 10. Mở rộng editor/admin để quản trị nội dung mới

- Nếu không có editor hoặc seed quản lý được nội dung mới thì frontend sẽ bị hardcode quá nhiều.
- Cần bổ sung chỗ quản trị cho:
- dashboard môn học
- thẻ giao tiếp
- game catalog
- nghề nghiệp
- media/video
- nội dung `Sắp ra mắt`

### Tối thiểu nên làm

- trước mắt seed cứng để nhanh ra giao diện
- sau đó mới thêm CMS/editor nếu sản phẩm chốt hướng

## 11. Chuẩn hóa asset và media

- Bộ ảnh demo dùng icon lớn, minh họa đồng nhất, hình rất “trẻ em”.
- Dự án hiện cần một bộ asset ổn định hơn nếu muốn lên giao diện theo hướng đó.

### Cần chuẩn bị

- icon môn học đồng bộ
- ảnh bài học đúng nội dung
- ảnh giao tiếp
- ảnh nghề nghiệp
- ảnh game
- background cho từng theme
- audio mẫu hoặc audio generate sẵn

## 12. Dọn nợ kỹ thuật trước khi refactor lớn

- `StudentHomePage.tsx` đang rất lớn, nếu sửa trực tiếp sẽ dễ vỡ.
- `LessonsPage.tsx` cũng đang quá to.
- Nên ưu tiên tách component trước hoặc vừa tách vừa chuyển giao diện.

### File sẽ bị ảnh hưởng mạnh

- `frontend/src/pages/StudentHomePage.tsx`
- `frontend/src/components/activities/ActivityRenderer.tsx`
- `frontend/src/pages/LessonsPage.tsx`
- `frontend/src/services/api.ts`
- `backend/app/models/subject.py`
- `backend/app/services/seed_service.py`
- Có khả năng cần thêm route/model mới ở backend cho:
- giao tiếp
- game hub
- nghề nghiệp
- student preferences

## Gợi ý lộ trình triển khai

### Pha 1 - Chốt cấu trúc và dựng UI học sinh mới

- chốt 3 tab chính
- thêm màn chọn mức độ
- refactor `StudentHomePage`
- dựng banner + subject grid + section game/nghề nghiệp bằng dữ liệu mock

### Pha 2 - Mở rộng dữ liệu backend

- mở rộng `subjects`
- seed thêm môn
- thêm cấu hình hiển thị môn
- thêm seed visual content đúng nội dung

### Pha 3 - Tách `Giao tiếp`, `Trò chơi`, `Nghề nghiệp`

- làm trang giao tiếp riêng
- làm game launcher riêng
- làm career hub + career detail

### Pha 4 - Nâng editor và hoàn thiện media

- mở rộng lesson/content editor
- xử lý upload / embed / validate video
- dọn nội dung demo sai hoặc thiếu

### Pha 5 - Kiểm thử thực tế

- test luồng học sinh từ đăng nhập
- test audio
- test mobile
- test video nhúng
- test trạng thái `Sắp ra mắt`
- test realtime và không để refactor mới làm hỏng luồng assignment cũ

## Kết luận thực dụng

- Hướng trong ảnh demo không phải chỉ là “đổi giao diện”.
- Đây là đổi cả mô hình trải nghiệm học sinh:
- từ assignment-first
- sang visual learning hub
- Dự án hiện tại có sẵn khoảng 50-60% nền kỹ thuật có thể tái sử dụng.
- Phần phải làm mới nhiều nhất là:
- kiến trúc student UX
- dashboard môn học
- giao tiếp độc lập
- game hub
- thư viện nghề nghiệp
- Nếu làm, nên coi đây là một nhánh refactor sản phẩm học sinh, không phải một chỉnh sửa nhỏ trong `StudentHomePage`.
